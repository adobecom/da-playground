.hlx/*
coverage/*
logs/*
node_modules/*
.DS_Store
.idea
.env
.github/workflows/import/node_modules/*
# Snowflake skill: in-progress run state (committed only on close)
.snowflake/projects/*/state.json
# DA auth token — written by forge at runtime, must never be committed
.snowflake/da-token.json
